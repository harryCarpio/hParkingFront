# Archivos que entran en un contenedor Linux: deben mantenerse con LF.
# Un CRLF en el entrypoint hace que /bin/sh falle con "\r: not found" y el
# contenedor no arranque.
*.sh text eol=lf
*.conf text eol=lf
Dockerfile text eol=lf
docker-compose.yml text eol=lf
