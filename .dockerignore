# Se instalan dentro de la imagen con npm ci.
node_modules

# La SPA se compila en la etapa 1; un dist/ local solo ensucia el contexto.
dist
dist-ssr

# Pesado y no hace falta: el hash del commit entra como build arg BUILD_COMMIT.
.git
.gitignore

# Secretos y configuracion local — nunca deben entrar en la imagen.
.env
.env.*
*.local

.vscode
.idea
.claude
*.log
logs
README.md
CLAUDE.md
